server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        # nacos
        server-addr: 192.168.0.103:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'